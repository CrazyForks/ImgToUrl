# 生产环境配置文件
# 请根据您的实际情况修改以下配置

# ===========================================
# 数据库配置
# ===========================================
DB_USER=imagehost
DB_PASSWORD=your_secure_password_here_change_this
DB_NAME=image_host

# ===========================================
# R2 存储配置（可选）
# 如果不配置，系统将使用本地存储
# ===========================================
# R2_ACCOUNT_ID=your_r2_account_id
# R2_ACCESS_KEY_ID=your_access_key_id
# R2_SECRET_ACCESS_KEY=your_secret_access_key
# R2_BUCKET_NAME=your_bucket_name
# R2_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
# R2_PUBLIC_URL=https://your_custom_domain.com

# ===========================================
# 文件上传限制
# ===========================================
MAX_FILE_SIZE=10485760        # 10MB (单个文件最大大小)
MAX_BATCH_SIZE=52428800       # 50MB (批量上传最大大小)
MAX_BATCH_COUNT=10            # 批量上传最大文件数量

# ===========================================
# 前端配置
# 请将 your_domain_or_ip 替换为您的域名或VPS的IP地址
# ===========================================
VITE_API_BASE_URL=http://your_domain_or_ip:8080

# ===========================================
# 服务器配置
# ===========================================
SERVER_PORT=8080
SERVER_MODE=release

# ===========================================
# 示例配置（请根据实际情况修改）
# ===========================================
# 如果您的域名是 img.example.com，配置如下：
# VITE_API_BASE_URL=https://img.example.com/api
# 
# 如果您的VPS IP是 123.456.789.0，配置如下：
# VITE_API_BASE_URL=http://123.456.789.0:8080